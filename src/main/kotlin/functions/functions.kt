package functions

fun main(){

`fun can be used`()

    multiply(5,7)

}

fun `fun can be used`(){

println("out of syllabus")
}

fun multiply(a:Int,b:Int) : Int{

    var b = b * 2
    var c = b * a
    println("b is inflated twice and the answer is $c when $a and $b are inputs")
    return c
}

